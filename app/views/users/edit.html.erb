<h1>マイページ編集</h1>
 <%= form_with model:@user, local:true do |f| %>
  <label>名前</label>
  <%= f.text_field :name %>
  <label>画像</label>
  <%= f.attachment_field :profile_image %>

  <%= f.submit "保存" %>
 <% end %>